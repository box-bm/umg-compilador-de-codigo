import { ASTNode } from "../types/AST";
import Token from "../types/Token";

const AnalizadorSintactico = (tokens: Token[][]): ASTNode => {
  throw new Error("Function not implemented.");
}

export default AnalizadorSintactico;